spring:
  cloud:
    config:
      # Specify configuration service to get it owner config
      discovery:
        service-id: oha-configuration-service
        enabled: true
      name: api-gateway
      profile: dev
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/